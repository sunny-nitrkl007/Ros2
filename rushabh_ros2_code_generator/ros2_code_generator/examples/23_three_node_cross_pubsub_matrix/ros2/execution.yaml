# ==============================================================================
# Execution grammar
# ------------------------------------------------------------------------------
# Keep execution single-threaded for this communication demo so generated main()
# remains simple and easy to inspect.
# ===============================================================================
execution:
  default:
    executor: single_threaded
    number_of_threads: 1
  node_overrides: {}
