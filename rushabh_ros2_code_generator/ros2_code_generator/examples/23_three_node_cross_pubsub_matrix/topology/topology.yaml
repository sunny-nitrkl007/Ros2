# ==============================================================================
# Topology grammar
# ------------------------------------------------------------------------------
# This example runs three generated nodes as separate processes.
# ===============================================================================
topology:
  mode: separate_processes
  description: node_a, node_b, and node_c communicate through string topics.
