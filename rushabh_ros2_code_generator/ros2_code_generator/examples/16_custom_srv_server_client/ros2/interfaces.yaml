interfaces:
  messages: []
  services:
    - name: SetMode
      request:
        - type: string
          name: mode
      response:
        - type: bool
          name: success
        - type: string
          name: message
