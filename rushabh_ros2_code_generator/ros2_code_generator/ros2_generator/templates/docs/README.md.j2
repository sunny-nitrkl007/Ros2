# {{ app.package_name }}

Generated by RSG V1 Core Option-B inline layout.

## Layout

```text
src/<node>.cpp
include/{{ app.package_name }}/<node>.hpp
```

Business logic placeholders are marked by `USER LOGIC START` comments inside the node `.cpp` files.

## Interfaces
{% if interfaces.messages %}
Messages:
{% for m in interfaces.messages %}
- `msg/{{ m.name }}.msg`
{% endfor %}
{% endif %}
{% if interfaces.services %}
Services:
{% for s in interfaces.services %}
- `srv/{{ s.name }}.srv`
{% endfor %}
{% endif %}
