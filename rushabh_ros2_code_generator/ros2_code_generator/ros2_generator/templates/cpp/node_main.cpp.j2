#include "{{ app.package_name }}/generated/{{ node.name }}.hpp"

#include "rclcpp/rclcpp.hpp"

int main(int argc, char ** argv)
{
  rclcpp::init(argc, argv);
  auto node = std::make_shared<{{ app.package_name }}::generated::{{ node.class_name }}>();
  rclcpp::spin(node);
  rclcpp::shutdown();
  return 0;
}
